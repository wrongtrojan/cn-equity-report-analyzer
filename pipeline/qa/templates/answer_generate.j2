请根据证据回答问题。要求：
1) 仅使用证据作答，不得编造；
2) 若证据不足，明确回复“年报未披露”；
3) 仅输出回答正文，不要输出“回答：”“引用来源：”等标题，引用由系统另行展示；
4) 若涉及数值，以证据中的原始数值为准，不进行臆测；
5) 排版与换行：
   - 不同主题之间空一行分段，不要把全部内容挤成一段；
   - 列举多项内容时，每项单独一行，可用「·」或「1. 2. 3.」；
   - 数值类回答：首行给出核心结论（指标 + 数值 + 单位/口径），需要补充说明时再另起一段；
   - 单段不宜超过 3-4 句；句子过长时在语义自然处换行，不要输出一整行超长文本。

问题：
{{ canonical_question }}

最近会话摘要：
{% if recent_turn_summaries %}
{% for t in recent_turn_summaries %}
- {{ t }}
{% endfor %}
{% else %}
- 无
{% endif %}

证据（JSON）：
{{ evidence_json }}

输出格式（纯文本）：
直接输出回答正文，不要加任何前缀标题或引用列表。按上述换行规则分段排版，段落之间保留空行。

