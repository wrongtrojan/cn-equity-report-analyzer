请根据证据回答问题。要求：
1) 仅使用证据作答，不得编造；
2) 置信度策略：
   - 证据直接支持结论 → confidence_level=high，给出明确回答；
   - 证据部分相关、可合理推断 → 仍给出最佳答案（或说明已知线索），confidence_level=medium 或 low，reason 说明依据与缺口；
   - 仅当证据列表为空时 → confidence_level=none，回答「年报未披露」；
   - 若已检索到证据但无法直接作答，不得标 none；应标 low/medium，并在 answer 或 reason 中说明已找到的相关线索；
3) 若涉及数值，以证据中的原始数值为准，不进行臆测；
4) 排版与换行：
   - 不同主题之间空一行分段，不要把全部内容挤成一段；
   - 列举多项内容时，每项单独一行，可用「·」或「1. 2. 3.」；
   - 数值类回答：首行给出核心结论（指标 + 数值 + 单位/口径），需要补充说明时再另起一段；
   - 综合性/多子问题：先给 1-2 句总括，再分点展开；允许较长回答，按子问题分段，每段 2-5 句均可；
   - 若问题包含多个方面（如「营收和净利润」），逐方面作答，不要只答其一。

{% if is_comprehensive %}
5) 本题为综合性问题（intent={{ intent }}）：
   - 尽量整合多条证据，形成结构化回答；
   - 某一方面证据不足时，对该方面单独说明缺口，其他有证据的方面仍应完整回答；
   - 置信度通常应为 medium，除非多源证据高度一致且覆盖全面。
{% endif %}

问题：
{{ canonical_question }}

最近会话摘要：
{% if recent_turn_summaries %}
{% for t in recent_turn_summaries %}
- {{ t }}
{% endfor %}
{% else %}
- 无
{% endif %}

证据（JSON）：
{{ evidence_json }}

输出格式（仅 JSON，不要 markdown 代码块）：
{
  "answer": "回答正文，按上述换行规则分段排版",
  "confidence_level": "high|medium|low|none",
  "confidence_score": 0.0,
  "reason": "置信度说明，可为空字符串"
}
